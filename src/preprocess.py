"""Pre-processing module (auto-generated skeleton)."""

import logging
from typing import Tuple

logger = logging.getLogger(__name__)


def get_dataloaders(*_args, **_kwargs) -> Tuple[object, object]:  # type: ignore
    """Return empty dataloaders – placeholder."""

    raise NotImplementedError(
        "No preprocessing implementation available – original Experiment Code was not provided."
    )
